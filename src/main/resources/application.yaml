server:
    port: 8080
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/hotelmanager?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
        username: root
        password: Admin123!
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                format-sql: true
        show-sql: true
        database: MYSQL
        hibernate:
            ddl-auto: update
